**/node_modules
**/dist
**/.next
**/coverage
**/*.log
.git
.github
.env
.env.*
!.env.example
docs
# Test files are kept in the build context so the `build` stage can run the
# suite (CI parity). They are never copied into the `runtime` image.
